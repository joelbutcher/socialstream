providers:
  - Laravel\Socialite\SocialiteServiceProvider
  - JoelButcher\Socialstream\SocialstreamServiceProvider

migrations:
  - database/migrations

workbench:
  start: '/'
  install: true
  build:
    - create-sqlite-db
    - db:wipe
    - migrate:refresh
